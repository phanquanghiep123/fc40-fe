import { PATH_GATEWAY } from '../../../utils/request';

export const API_LINK = {
  GET_BY_ID: `${PATH_GATEWAY.RESOURCEPLANNING_API}/master-data-suppliers`, // {id}
  CREATE_ITEM: `${PATH_GATEWAY.RESOURCEPLANNING_API}/master-data-suppliers`,
  UPDATE_ITEM: `${PATH_GATEWAY.RESOURCEPLANNING_API}/master-data-suppliers`, // {id}
};
