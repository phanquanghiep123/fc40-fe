/*
 * InventoryBasket Messages
 *
 * This contains all the text for the InventoryBasket container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InventoryBasket';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InventoryBasket container!',
  },
});
