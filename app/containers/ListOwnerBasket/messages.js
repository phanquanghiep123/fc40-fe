/*
 * ListOwnerBasket Messages
 *
 * This contains all the text for the ListOwnerBasket container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListOwnerBasket';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListOwnerBasket container!',
  },
});
